# Pipeline scripts (bundled)

Drop or commit `.aether-pipeline.json` / `.yaml` files here.  
Hub loads them on start; users can also `POST /api/pipelines/import`.

See [docs/PIPELINES.md](../../docs/PIPELINES.md).
